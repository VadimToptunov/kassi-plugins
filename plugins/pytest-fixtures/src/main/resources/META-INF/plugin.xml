<idea-plugin>
    <id>io.github.vadimtoptunov.pytest-fixtures</id>
    <name>pytest Fixtures - Valid Test Data</name>
    <vendor email="vtoptunov88@gmail.com" url="https://github.com/VadimToptunov/kassi-plugins">Vadim Toptunov</vendor>

    <description><![CDATA[
    <h2>pytest Fixtures — valid test data</h2>
    <p>
    Generate a <b>pytest parametrized test</b> whose cases are <b>spec-valid</b> values — and their invalid
    variants — straight from the Kassi engine. Unlike a hand-rolled faker, every value passes the real
    checksum, so your test exercises the validator with data that is actually valid (and edge cases that are
    actually invalid). Fully offline.
    </p>
    <ul>
      <li><b>IBAN</b> — ISO 7064 mod-97 valid, several countries, each with a bad-checksum variant.</li>
      <li><b>Card (PAN)</b> — Luhn-valid per network, each with a fails-Luhn variant.</li>
    </ul>
    <p>Open it from <b>View ▸ Tool Windows ▸ pytest Fixtures</b>, pick a kind, <b>Generate</b>, then <b>Copy</b>
    the <code>@pytest.mark.parametrize</code> test into your package. Platform-only — installs in PyCharm and
    every IntelliJ-based IDE.</p>
    <p><b>More Kassi tools for QA &amp; fintech:</b>
    <a href="https://plugins.jetbrains.com/plugin/33149">Kassi Test Data</a>,
    <a href="https://plugins.jetbrains.com/plugin/33227">Checksum &amp; Regex Playground</a>.</p>
    <p><b>Source, issues &amp; docs:</b> <a href="https://github.com/VadimToptunov/kassi-plugins">github.com/VadimToptunov/kassi-plugins</a></p>
    ]]></description>

    <change-notes><![CDATA[
    <h3>1.0.0</h3>
    <ul><li>First release: generate pytest parametrized tests for IBAN and Card (PAN) validation — valid values and their invalid variants, from the checksummed Kassi engine.</li></ul>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow id="pytest Fixtures"
                    anchor="right"
                    secondary="true"
                    factoryClass="io.github.vadimtoptunov.pytestfixtures.PytestFixturesToolWindowFactory"
                    icon="/icons/pytestFixturesToolWindow.svg"/>
    </extensions>
</idea-plugin>
